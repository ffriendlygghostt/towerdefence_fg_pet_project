using UnityEngine;

public class BootCamera : MonoBehaviour
{
    private static BootCamera instance;

    private void Awake()
    {
        if (instance != null)
        {
            Destroy(gameObject);
            return;
        }

        instance = this;
        DontDestroyOnLoad(gameObject);
    }
}
